
import classe.*;
import repository.*;

import java.io.IOException;
import java.util.ArrayList;

public class Main {
    public static void main(String[] args) throws IOException {

        /*ArrayList<Lehrer> lehrerList = null;
        ArrayList<Student> studentList = null;
        ArrayList<Kurs> kursList = null;

        Lehrer lehrer = new Lehrer("Ana", "Popescu", kursList);
        Student student = new Student("Georgiana", "Iepan", 125, 30, kursList);
        Kurs kurs = new Kurs("MAP", lehrer, 150, studentList, 6);

        RegistrationSystem registrationSystem = new RegistrationSystem();

        registrationSystem.getLehrerFinalList().create(lehrer);
        registrationSystem.getStudentFinalList().create(student);
        registrationSystem.getCourseFinalList().create(kurs);

        for (Lehrer l : registrationSystem.getLehrerFinalList().getAll()) {
            System.out.println(l);
        }

        for (Student s : registrationSystem.getStudentFinalList().getAll()) {
            System.out.println(s);
        }

        for (Kurs k : registrationSystem.getCourseFinalList().getAll()) {
            System.out.println(k);
        }

        registrationSystem.register(kurs, student);

        //registrationSystem.retrieveCoursesWithFreePlaces();

        //registrationSystem.retrieveStudentsEnrolledForACourse(kurs);

        for (Kurs k : registrationSystem.getAllCourses()) {
            System.out.println(k);
        }*/

    }
}
